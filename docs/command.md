-- Используем исключительно компоненты из Naive UI. Минимизируем самостоятельную верстку компонентов и минимизируем чистый HTML.

-- Не забудь НЕ использовать deep. Только встроенные возможности Naive. Если что-то нельзя сделать, оставляем дефолтные возможности Naive.

-- Максимально стараемся кастомизировать через встроенные Naive механизмы. В CSS дорабатываем в самом крайнем случае.

-- Если я прислал ссылку на фигму и по какой-то причине доступа к макету нет, ничего не реализовывай, просто сообщи о проблеме с доступом в фигме.
